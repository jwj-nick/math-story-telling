<!-- 260818_math1_upgrade.md -->

# math1 전면 재설계 — math2(app2) 수준으로

## Round 0 — 착수 결정 (2026-08-18)

**Nick 요청**: "math1을 많이 수정하더라도 math2 2학기 수준으로 업그레이드 필요함. 기존 math1에 아이가 큰 관심은 없었고, 2학기때는 시험도 치며, math2의 프린트 버전도 있으면 좋겠음. 계획 알려주기바람."

**NCC 사전 조사**:
- math2 프린트는 이미 72개 PDF 배포 완료(`jwj-nick.github.io/mid2/print/`) — 별도 작업 불필요로 판단, Nick에게 확인 질문.
- math1 프린트도 이미 234개 PDF 배포 완료(`jwj-nick.github.io/mid1/math1/print/`) — 다만 위치가 `mid1/print/`가 아니라 `mid1/math1/print/`라 처음엔 못 찾음.
- math1은 13단원 각자 독립 정적 HTML(concepts+index+basic_app+type_01~07+walk_01~07_H1~H3+deep_*, 단원당 28개+ 파일), 공유 엔진 없음. `geo.js`(MG)는 `MJ.makeGeo`와 API 호환 확인(트랙B 리서치 때 이미 확인한 사실 재확인).

**Q&A (AskUserQuestion)**:
1. 재설계 깊이 — 기존 콘텐츠 재포장 vs 전면 재설계 → **전면 재설계**
2. 순서 — 파일럿 먼저/2학기 먼저/1학기부터 순서대로 → **2학기(07~13) 먼저**
3. "math2 프린트 버전" 확인 — 이미 있음/새 놀이터용 프린트 원함/다른 의미 → **"math1 2학기 프린트 있어야함"**. 즉 math2 프린트가 아니라, **math1 2학기 콘텐츠가 전면 재설계되니 그에 맞는 프린트를 새로 만들어야 한다**는 뜻으로 확인.

**결정**: `00_project_hub/20_plan/0818_math1_upgrade_plan.md` 작성 완료. 요약:
- 공유 엔진 `concept.js`/`concept.css`를 math1 전용으로 포크(math2와 공유 안 함, 자기완결 원칙)
- 탭 체계 = u(개념)→g(놀이터)→p(연습)→d(심화탐구) 4탭. x(PhET)·dp(심화문제)는 1차 제외
- 콘텐츠 압축: concepts+basic_app+type_NN(7)+walk_NN_H1~H3(21) = 29개 파일 → u{N}+p{N} 2개로. deep_*+explore(geo.js 위젯) → d{N} 1개로
- 놀이터(g)는 07~12는 기존 카탈로그(`0811_interest_playground_plan.md` §2b) 재사용, 13(자료의 정리와 해석)은 신규 아이디어 필요
- 2학기(07~13) 콘텐츠 재설계 후 프린트도 `/se-print-sheet`로 재생성
- **P1(단원07 파일럿)이 게이트** — 파일럿 완성 후 Nick 검수, 패턴 확정 후 나머지 12단원 순차

**다음**: P0(엔진 스캐폴딩) → P1(단원07 기본도형 파일럿: u7+p7+d7+g7+프린트) 착수.

## Round 1 — P0+P1 완료: 단원7(기본도형) 파일럿 (2026-08-19)

- **P0 스캐폴딩**: `math1/app1/concept.js`+`concept.css` — math2 `concept.js`/`concept.css` 전문을 읽어 그대로 포크. `MJ1` 네임스페이스, 13단원 UNITS(색상 토큰 신규), localStorage 키를 `m1theme`/`m1progress`로 분리(동일 origin에 math2가 `m2*` 쓰므로 충돌 방지 — 이번에 처음 발견하고 고려한 이슈). 탭 체계는 u/g/p/d 4개로 확정(x·dp 1차 제외, 계획서 §2-2 그대로).
- **허브**: `app1/index.html` 신설 — 1학기 6단원 + 2학기 8~13단원은 "준비중" placeholder(링크 없음, 404 방지), 단원7만 실제 링크. 기존 `math1/index.html`(구버전)은 그대로 두고 배너 한 줄만 추가.
- **P1 파일럿(단원7 · 기본도형) 4페이지 전부 완성**:
  - `u7.html`(개념, 8섹션): 이어서(점선면) → 직선·반직선·선분(정적 3분할 SVG) → **각도 슬라이더**(예각/직각/둔각 실시간 분류, 신규 구현) → **맞꼭지각 슬라이더**(기울기 조절, 4개 각 실시간 재계산 — `angle()` 함수 호출 시 vertex 인자 순서를 미리 4방향 다 손으로 검증) → 동위각·엇각(정적 다이어그램) → 위치관계(직육면체, 꼬인위치) → 내 말로 → 다음(g7로 브릿지).
  - `g7.html`(놀이터, 4위젯): 위치관계 판별사(직육면체 모서리 12개 좌표 직접 계산, 평행/만남/꼬인위치 4라운드) / 각 짝꿍 찾기(8각 다이어그램, 동위각·엇각 짝 4라운드 — 옛 concepts.html의 라벨 배치 로직·페어링표를 검증된 그대로 재사용) / 맞꼭지각 vs 이웃각 감별사(4라운드) / 오류 탐정(`math1_u07.json` 15개 중 6개 큐레이션, g7 자체 위젯과 겹치는 항목 제외).
  - `p7.html`(연습): types.md의 7유형을 그대로 유지하되, 유형당 9문제(L/M/H×3, 옛 walk_NN_H1~H3)를 **3문제로 압축**(math2 p7.html의 mission/prob/pick 패턴 그대로 이식) — 21문제. 옛 `basic_app+type_01~07+type_app+walk_01~07_H1~H3` 29개 파일을 이 1페이지로 대체.
  - `d7.html`(심화탐구): "꺾인 선 사이의 각 — 보조선"을 단일 주제로(math2 d7.html의 "외심이 언제 밖으로?" 패턴처럼 하나에 집중). 위쪽각·아래쪽각 슬라이더 + 실시간 합산 표시. 좌표 공식(각도→dx 변환)을 손으로 벡터 내적 검증(70°=40+30 확인) 후 구현. `par.auxiliary-line`(보조선 못 떠올림) 오개념 정면 타격 — g7/u7/p7 어디서도 안 쓴 새 소재.
- **검증**: 4페이지 전부 node 구문검사 OK, id 참조 0 누락(p7은 동적 `P()`/`pick()` 호출이라 별도 스크립트로 문자열 추출 후 교차검증), null-byte 0. **Chrome 확장이 이번엔 계속 연결 유지** — index/u7/g7/p7/d7 5개 페이지 전부 리로드 후 콘솔에러 0건 실측 확인(스크린샷 자체는 여전히 타임아웃, 환경 이슈 지속이나 콘솔체크로 충분한 신뢰도 확보).
- **⚠️ 배포 사고 발견·즉시 복구**: 소스 `math1/index.html`을 그대로 배포 repo에 복사했다가, 그 소스 파일이 `20_packages/design-system` CSS(배포 repo엔 존재하지 않는 경로)를 참조하는 **오래전부터 미완성이던 리디자인**이었음을 뒤늦게 발견(다른 배포 페이지 어디에도 20_packages 참조 없음을 확인해서 교차검증). 실제 라이브였던 건 인라인 스타일의 카드 그리드 버전. git history(`HEAD~1`)에서 그 원본을 복원하고, 그 위에 배너 한 줄만 얹어 재배포. **소스 파일 자체의 드리프트는 이번 세션이 만든 게 아니라 이전부터 있던 상태**이며, 고치는 건 이번 스코프 밖이라 손대지 않음(다음에 이 파일을 다시 만질 일이 있으면 먼저 배포본과 diff부터 확인할 것).
- 소스(`math-story-telling` `52ea54a`)·배포(`jwj-nick.github.io` `f4e8503`, 복구 커밋 포함) 양쪽 push 완료. 라이브 확인: https://jwj-nick.github.io/mid1/math1/app1/g7.html 등 5개 페이지 전부 200 + 본문 크로스체크 통과.
- **다음**: Nick의 P1.5 파일럿 리뷰 대기(압축 강도·톤·놀이터 난이도가 기대와 맞는지). 승인 후 (a) 단원7 프린트 재생성(`/se-print-sheet`) → (b) 단원8~13 순차 재설계로 진행.

## Round 2 — 단원7 파일럿 승인 + 프린트 재생성 (2026-08-20)

- **Nick 승인**: "단원7 괜찮음. 나머지 단원 모두 진행바람" — P1.5 게이트 통과, P2(단원8~13) 착수 승인.
- **프린트 요청**: "프린트는 너무 어려운 문제만 아니면 괜찮음. 기초문제 위주로 일단 만들어주길. 심화는 나중에 추가요청하겠음" → `/se-print-sheet` 스킬로 단원7 프린트를 새 p7.html 기준으로 재생성.
- **STEP1 추출**: `extract_math2_p.py`를 math1 app1/p7.html에 **그대로 재사용**(마크업이 math2 세대D와 100% 동일 패턴 — MJ1.check 호출은 함수 정의 내부라 정규식과 무관, 실제 파싱 대상인 `prob()/P()/pick()` 리터럴 호출은 이름 그대로 매치). `types=7 problems=21 atoms=21 unmatched=0 choice2=9`.
- **STEP2 지면 저작**: `30_content/problem_bank/math1/u07/practice.json` 21문항 전부 L/M만(심화 제외, Nick 요청 그대로). 2지선다 9개를 math2 템플릿의 변환 관행(예/아니오 판단 → 실제 개념을 묻는 단답)대로 재작성.
  - **verify_bank V5 FAIL 2건 발견·수정**: (1) `fig.ray-direction` 오개념 카드의 반례 예시가 우연히 "반직선 PQ"를 언급하는데 문항 답도 "반직선 PQ"라 누출 판정 → 점 이름을 P,Q에서 A,B로 바꿔 해결. (2) `ang.type-range` 오개념 카드는 예각/직각/둔각/평각 네 낱말을 전부 반례에 나열하는 카드라 "무슨 각인가요" 형태 문항은 어떤 답을 넣어도 누출 → 문항을 "예각의 경계값(90°)을 구하시오"로 재설계해 낱말 자체를 답에서 제거.
- **오개념 카탈로그**: 기존 `math1_u07.json`(15개) 그대로 재사용, 새로 안 씀(STEP3 생략).
- **STEP4 회차**: math2식 3문항/유형 규칙 적용 — a(유형1-3, 9문항)·b(유형4-6, 9문항)·c(유형7, 3문항). **모의고사 회차는 생성 안 함**(Nick "심화는 나중에").
- **STEP5 검증**: `verify_bank.py` 오류 0(경고 11 — 전부 재작성 문항의 예상된 형태이거나 이번 회차에 안 쓴 오개념 4개 안내, 문제 없음).
- **STEP6~7**: PDF 6개(회차 3개 × Q/K) headless Chrome으로 생성, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 — 2단 레이아웃·난이도 점(●○○/●●○)·풀이공간 크기(sm/md/lg)·오개념 카드 정답 비노출 전부 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계로 만들어졌던 PDF 18개(t1~t7 + mockA/B)와 그 HTML/pdfplan 잔재를 삭제, 새 3개 회차로 완전 교체.
- **배포**: 소스(`math-story-telling` `beacc65`) 커밋. 배포(`jwj-nick.github.io` `212f4fc`) — PDF 복사 + `mid1/math1/print/index.html`의 단원7 링크를 새 회차명으로 갱신(node 스크립트로 정밀 치환, null-byte 0 확인). Pages 라이브 확인(raw.githubusercontent.com 200 → CDN 전파 대기 후 jwj-nick.github.io도 200).
  - ⚠️ 배포 repo에 이번 세션과 무관한 `high1/`(다른 프로젝트) 로컬 변경분이 있었음 — `git add`를 파일 단위로 정확히 지정해 건드리지 않고 넘어감.
  - 참고: `mid1/math1/print/index.html`(단원별 프린트 브라우징 페이지)은 소스 저장소에 대응 파일이 없는 **배포 전용 파일**임을 확인(생성 스크립트도 없음) — 이번엔 배포본만 직접 수정. 다음에 프린트 재생성할 때도 이 파일은 배포 repo에서만 갱신하면 됨.
- **다음**: 단원8(작도와 합동)부터 순차 재설계 착수 — u/g/p/d 4페이지 + 프린트, 단원7과 동일 패턴.

## Round 3 — 단원8(작도와 합동) 완성 (2026-08-20)

- Nick "나머지 단원 모두 진행바람" 승인에 따라 단원8을 단원7과 동일 패턴(u/g/p/d + 프린트)으로 완성.
- **u8.html(개념 8섹션)**: 도구역할→길이옮기기→**수직이등분선 슬라이더**(반지름 조절, 두 호 교점 실시간 계산)→각의이등분선(정적 SSS 증명 다이어그램)→삼각형작도조건(SSS/SAS/ASA + AAA/SSA 함정 텍스트)→합동표기순서(정적 대응 다이어그램)→내말로→다음.
- **g8.html(놀이터 4위젯)**: 합동판정관(SSS/SAS/ASA/SSA/AAA 5라운드) / 끼인각찾기(`MJ1.makeGeo`로 삼각형 그려 두 변 강조, 3라운드) / 컴퍼스작도시뮬레이터(반지름 슬라이더로 두 호의 교점 개수 실시간, AB=7cm 절반=3.5cm 챌린지) / 오류탐정(`math1_u08.json` 6종 큐레이션).
- **p8.html(연습 7유형×3=21문항)**: 작성 중 **논리 오류 1건 자체 발견·수정** — 유형2 문항(2) "벌림을 바꾸면 바뀔까요?"인데 정답이 "안 바뀌어요"로 되어 있어 질문·답이 모순이었음(원래 의도는 "벌림을 유지하면 항상 같은가"였는데 문구가 반대로 나감) → 질문을 "벌림을 유지하면 항상 같을까요?"로 정정, 앱·프린트 모두 재검증.
- **d8.html(심화탐구)**: "각의 삼등분은 왜 안 될까" — 이등분 반복 슬라이더(90°÷2ⁿ), `constr.trisection` 오개념 정면 타격.
- **검증**: p8.html에서 **버그 1건 자체 발견·수정** — `pick('c1','p21a'.replace('2','1'),...)` 문자열 표현식 잔재(의도는 `'p12a'`)가 남아있어 클릭 핸들러가 엉뚱한 버튼(`p11a`, 이미 사용 중)에 붙는 실제 오류였음 → 리터럴로 교체. 4페이지 전부 구문/id/null-byte 통과, Chrome 확장 연결되어 콘솔에러 0건 실측.
- **프린트**: `extract_math2_p.py` 재사용(unmatched=0, choice2=12). verify_bank V5 FAIL 2건 — `constr.tool-role` 오개념 카드가 counter/ask에서 "컴퍼스"를 실명으로 여러 번 거론해서, 답이 "컴퍼스"인 문항 2개와 충돌 → 두 문항의 misconceptions 태그만 제거(모든 문항에 오개념이 꼭 붙어야 하는 건 아님)로 해결. 회차 3개(a:유형1-3, b:유형4-5, c:유형6-7) 생성, PDF 6개, 2장 육안 확인 정상.
- **배포**: 소스(`math-story-telling`, 앱 `89da4e3`+버그수정 `904ee17`, 프린트 `b39ce7e`) · 배포(`jwj-nick.github.io`, 앱 `e8de76f`+수정 `0bdb638`, 프린트 `80dac32`) 전부 push 완료. `high1/`(무관한 다른 프로젝트) 로컬 변경분은 파일 단위 staging으로 계속 건드리지 않음.

## Round 4 — 단원9(다각형) 완성 (2026-08-21)

- 단원7·8과 동일 패턴(u/g/p/d + 프린트)으로 단원9 완성.
- **u9.html(개념 8섹션)**: 다각형정의(곡선판별)→구성요소(변=꼭짓점수)→**직접움직여봐요**(n슬라이더 4-8, 전체 대각선 세트를 이중루프로 실시간 렌더 + n(n−3)÷2 카운트)→삼각형내각외각(외각의 정리)→n각형내각의합(180×(n−2))→외각의합=360(항상성 강조)→내말로→다음.
- **g9.html(놀이터 4위젯)**: 대각선사냥꾼(정오각형 고정, 4라운드 변/대각선 판별 + 최종 "몇 개?" 챌린지) / 내각의합실험실(n슬라이더 3-8, 부채꼴 삼각분할 시각화 + 실시간 공식) / 외각레이서(n슬라이더 3-10, 항상 360° 강조) / 오류탐정(`math1_u09.json` 6종 큐레이션).
- **d9.html(심화탐구)**: "왜 정삼각형·정사각형·정육각형만 바닥을 채울까" — n슬라이더(3-10)로 정n각형 내각을 한 점에 부채꼴로 모으는 시뮬레이션, 360÷내각이 정수인지 실시간 판정(n=3,4,6만 성립 확인).
- **검증**: 4페이지 전부 구문/id/null-byte 통과, Chrome 확장 콘솔에러 0건 실측. 이번 회차는 로직 버그 없이 1차 작성에서 통과(u8의 논리모순·문자열치환 잔재 같은 이슈 재발 없음).
- **엔진 연결**: `concept.js` `PLAY_READY`에 9 추가, 허브 `index.html` 단원9 카드 활성화·캐시버전 `20260820b`·푸터 "단원7~9 오픈"로 갱신.
- **프린트**: `extract_math2_p.py` 재사용(`unmatched=0, choice2=1`). `30_content/problem_bank/math1/u09/practice.json` 21문항 신규 저작(전부 L/M, 2지선다 1문항 단답 재작성 — "원은 다각형인가요?"→"아닌지 쓰시오"). 기존 `misconceptions/math1_u09.json`(15개, 2026-07-29 기저작) 그대로 재사용. **이번엔 저작 전에 V5 답 누출을 13개 태그 전부 사전 점검**(u07/u08에서 걸렸던 실수 재발 방지) — verify_bank 1회 실행에 오류 0 (경고 4: 예/아니오 문항 1개의 V3/V4 경고 — u08 t01-03과 동일 패턴, 미사용 오개념 2개 안내). 회차 3개(a:유형1-3, b:유형4-6, c:유형7) 생성, PDF 6개, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 정상 — 2단 레이아웃·풀이공간·오개념 카드 정답 비노출 전부 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계 PDF 18개(t1~t7 + mockA/B)를 소스·배포 양쪽에서 삭제, 새 3개 회차로 완전 교체.
- **배포**: 소스(`math-story-telling`, 앱 `cad51f0`, 프린트 `df6b80a`) · 배포(`jwj-nick.github.io`, 앱 `e40e7ae`, 프린트 `e72be2b`) 전부 push 완료. `high1/` 무관 변경분은 이번에도 건드리지 않음.
- **다음**: 단원10(원과 부채꼴)부터 순차 진행.
- **다음**: 단원9(다각형)부터 이어서 진행.

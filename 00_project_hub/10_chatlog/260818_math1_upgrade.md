<!-- 260818_math1_upgrade.md -->

# math1 전면 재설계 — math2(app2) 수준으로

## Round 0 — 착수 결정 (2026-08-18)

**Nick 요청**: "math1을 많이 수정하더라도 math2 2학기 수준으로 업그레이드 필요함. 기존 math1에 아이가 큰 관심은 없었고, 2학기때는 시험도 치며, math2의 프린트 버전도 있으면 좋겠음. 계획 알려주기바람."

**NCC 사전 조사**:
- math2 프린트는 이미 72개 PDF 배포 완료(`jwj-nick.github.io/mid2/print/`) — 별도 작업 불필요로 판단, Nick에게 확인 질문.
- math1 프린트도 이미 234개 PDF 배포 완료(`jwj-nick.github.io/mid1/math1/print/`) — 다만 위치가 `mid1/print/`가 아니라 `mid1/math1/print/`라 처음엔 못 찾음.
- math1은 13단원 각자 독립 정적 HTML(concepts+index+basic_app+type_01~07+walk_01~07_H1~H3+deep_*, 단원당 28개+ 파일), 공유 엔진 없음. `geo.js`(MG)는 `MJ.makeGeo`와 API 호환 확인(트랙B 리서치 때 이미 확인한 사실 재확인).

**Q&A (AskUserQuestion)**:
1. 재설계 깊이 — 기존 콘텐츠 재포장 vs 전면 재설계 → **전면 재설계**
2. 순서 — 파일럿 먼저/2학기 먼저/1학기부터 순서대로 → **2학기(07~13) 먼저**
3. "math2 프린트 버전" 확인 — 이미 있음/새 놀이터용 프린트 원함/다른 의미 → **"math1 2학기 프린트 있어야함"**. 즉 math2 프린트가 아니라, **math1 2학기 콘텐츠가 전면 재설계되니 그에 맞는 프린트를 새로 만들어야 한다**는 뜻으로 확인.

**결정**: `00_project_hub/20_plan/0818_math1_upgrade_plan.md` 작성 완료. 요약:
- 공유 엔진 `concept.js`/`concept.css`를 math1 전용으로 포크(math2와 공유 안 함, 자기완결 원칙)
- 탭 체계 = u(개념)→g(놀이터)→p(연습)→d(심화탐구) 4탭. x(PhET)·dp(심화문제)는 1차 제외
- 콘텐츠 압축: concepts+basic_app+type_NN(7)+walk_NN_H1~H3(21) = 29개 파일 → u{N}+p{N} 2개로. deep_*+explore(geo.js 위젯) → d{N} 1개로
- 놀이터(g)는 07~12는 기존 카탈로그(`0811_interest_playground_plan.md` §2b) 재사용, 13(자료의 정리와 해석)은 신규 아이디어 필요
- 2학기(07~13) 콘텐츠 재설계 후 프린트도 `/se-print-sheet`로 재생성
- **P1(단원07 파일럿)이 게이트** — 파일럿 완성 후 Nick 검수, 패턴 확정 후 나머지 12단원 순차

**다음**: P0(엔진 스캐폴딩) → P1(단원07 기본도형 파일럿: u7+p7+d7+g7+프린트) 착수.

## Round 1 — P0+P1 완료: 단원7(기본도형) 파일럿 (2026-08-19)

- **P0 스캐폴딩**: `math1/app1/concept.js`+`concept.css` — math2 `concept.js`/`concept.css` 전문을 읽어 그대로 포크. `MJ1` 네임스페이스, 13단원 UNITS(색상 토큰 신규), localStorage 키를 `m1theme`/`m1progress`로 분리(동일 origin에 math2가 `m2*` 쓰므로 충돌 방지 — 이번에 처음 발견하고 고려한 이슈). 탭 체계는 u/g/p/d 4개로 확정(x·dp 1차 제외, 계획서 §2-2 그대로).
- **허브**: `app1/index.html` 신설 — 1학기 6단원 + 2학기 8~13단원은 "준비중" placeholder(링크 없음, 404 방지), 단원7만 실제 링크. 기존 `math1/index.html`(구버전)은 그대로 두고 배너 한 줄만 추가.
- **P1 파일럿(단원7 · 기본도형) 4페이지 전부 완성**:
  - `u7.html`(개념, 8섹션): 이어서(점선면) → 직선·반직선·선분(정적 3분할 SVG) → **각도 슬라이더**(예각/직각/둔각 실시간 분류, 신규 구현) → **맞꼭지각 슬라이더**(기울기 조절, 4개 각 실시간 재계산 — `angle()` 함수 호출 시 vertex 인자 순서를 미리 4방향 다 손으로 검증) → 동위각·엇각(정적 다이어그램) → 위치관계(직육면체, 꼬인위치) → 내 말로 → 다음(g7로 브릿지).
  - `g7.html`(놀이터, 4위젯): 위치관계 판별사(직육면체 모서리 12개 좌표 직접 계산, 평행/만남/꼬인위치 4라운드) / 각 짝꿍 찾기(8각 다이어그램, 동위각·엇각 짝 4라운드 — 옛 concepts.html의 라벨 배치 로직·페어링표를 검증된 그대로 재사용) / 맞꼭지각 vs 이웃각 감별사(4라운드) / 오류 탐정(`math1_u07.json` 15개 중 6개 큐레이션, g7 자체 위젯과 겹치는 항목 제외).
  - `p7.html`(연습): types.md의 7유형을 그대로 유지하되, 유형당 9문제(L/M/H×3, 옛 walk_NN_H1~H3)를 **3문제로 압축**(math2 p7.html의 mission/prob/pick 패턴 그대로 이식) — 21문제. 옛 `basic_app+type_01~07+type_app+walk_01~07_H1~H3` 29개 파일을 이 1페이지로 대체.
  - `d7.html`(심화탐구): "꺾인 선 사이의 각 — 보조선"을 단일 주제로(math2 d7.html의 "외심이 언제 밖으로?" 패턴처럼 하나에 집중). 위쪽각·아래쪽각 슬라이더 + 실시간 합산 표시. 좌표 공식(각도→dx 변환)을 손으로 벡터 내적 검증(70°=40+30 확인) 후 구현. `par.auxiliary-line`(보조선 못 떠올림) 오개념 정면 타격 — g7/u7/p7 어디서도 안 쓴 새 소재.
- **검증**: 4페이지 전부 node 구문검사 OK, id 참조 0 누락(p7은 동적 `P()`/`pick()` 호출이라 별도 스크립트로 문자열 추출 후 교차검증), null-byte 0. **Chrome 확장이 이번엔 계속 연결 유지** — index/u7/g7/p7/d7 5개 페이지 전부 리로드 후 콘솔에러 0건 실측 확인(스크린샷 자체는 여전히 타임아웃, 환경 이슈 지속이나 콘솔체크로 충분한 신뢰도 확보).
- **⚠️ 배포 사고 발견·즉시 복구**: 소스 `math1/index.html`을 그대로 배포 repo에 복사했다가, 그 소스 파일이 `20_packages/design-system` CSS(배포 repo엔 존재하지 않는 경로)를 참조하는 **오래전부터 미완성이던 리디자인**이었음을 뒤늦게 발견(다른 배포 페이지 어디에도 20_packages 참조 없음을 확인해서 교차검증). 실제 라이브였던 건 인라인 스타일의 카드 그리드 버전. git history(`HEAD~1`)에서 그 원본을 복원하고, 그 위에 배너 한 줄만 얹어 재배포. **소스 파일 자체의 드리프트는 이번 세션이 만든 게 아니라 이전부터 있던 상태**이며, 고치는 건 이번 스코프 밖이라 손대지 않음(다음에 이 파일을 다시 만질 일이 있으면 먼저 배포본과 diff부터 확인할 것).
- 소스(`math-story-telling` `52ea54a`)·배포(`jwj-nick.github.io` `f4e8503`, 복구 커밋 포함) 양쪽 push 완료. 라이브 확인: https://jwj-nick.github.io/mid1/math1/app1/g7.html 등 5개 페이지 전부 200 + 본문 크로스체크 통과.
- **다음**: Nick의 P1.5 파일럿 리뷰 대기(압축 강도·톤·놀이터 난이도가 기대와 맞는지). 승인 후 (a) 단원7 프린트 재생성(`/se-print-sheet`) → (b) 단원8~13 순차 재설계로 진행.

## Round 2 — 단원7 파일럿 승인 + 프린트 재생성 (2026-08-20)

- **Nick 승인**: "단원7 괜찮음. 나머지 단원 모두 진행바람" — P1.5 게이트 통과, P2(단원8~13) 착수 승인.
- **프린트 요청**: "프린트는 너무 어려운 문제만 아니면 괜찮음. 기초문제 위주로 일단 만들어주길. 심화는 나중에 추가요청하겠음" → `/se-print-sheet` 스킬로 단원7 프린트를 새 p7.html 기준으로 재생성.
- **STEP1 추출**: `extract_math2_p.py`를 math1 app1/p7.html에 **그대로 재사용**(마크업이 math2 세대D와 100% 동일 패턴 — MJ1.check 호출은 함수 정의 내부라 정규식과 무관, 실제 파싱 대상인 `prob()/P()/pick()` 리터럴 호출은 이름 그대로 매치). `types=7 problems=21 atoms=21 unmatched=0 choice2=9`.
- **STEP2 지면 저작**: `30_content/problem_bank/math1/u07/practice.json` 21문항 전부 L/M만(심화 제외, Nick 요청 그대로). 2지선다 9개를 math2 템플릿의 변환 관행(예/아니오 판단 → 실제 개념을 묻는 단답)대로 재작성.
  - **verify_bank V5 FAIL 2건 발견·수정**: (1) `fig.ray-direction` 오개념 카드의 반례 예시가 우연히 "반직선 PQ"를 언급하는데 문항 답도 "반직선 PQ"라 누출 판정 → 점 이름을 P,Q에서 A,B로 바꿔 해결. (2) `ang.type-range` 오개념 카드는 예각/직각/둔각/평각 네 낱말을 전부 반례에 나열하는 카드라 "무슨 각인가요" 형태 문항은 어떤 답을 넣어도 누출 → 문항을 "예각의 경계값(90°)을 구하시오"로 재설계해 낱말 자체를 답에서 제거.
- **오개념 카탈로그**: 기존 `math1_u07.json`(15개) 그대로 재사용, 새로 안 씀(STEP3 생략).
- **STEP4 회차**: math2식 3문항/유형 규칙 적용 — a(유형1-3, 9문항)·b(유형4-6, 9문항)·c(유형7, 3문항). **모의고사 회차는 생성 안 함**(Nick "심화는 나중에").
- **STEP5 검증**: `verify_bank.py` 오류 0(경고 11 — 전부 재작성 문항의 예상된 형태이거나 이번 회차에 안 쓴 오개념 4개 안내, 문제 없음).
- **STEP6~7**: PDF 6개(회차 3개 × Q/K) headless Chrome으로 생성, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 — 2단 레이아웃·난이도 점(●○○/●●○)·풀이공간 크기(sm/md/lg)·오개념 카드 정답 비노출 전부 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계로 만들어졌던 PDF 18개(t1~t7 + mockA/B)와 그 HTML/pdfplan 잔재를 삭제, 새 3개 회차로 완전 교체.
- **배포**: 소스(`math-story-telling` `beacc65`) 커밋. 배포(`jwj-nick.github.io` `212f4fc`) — PDF 복사 + `mid1/math1/print/index.html`의 단원7 링크를 새 회차명으로 갱신(node 스크립트로 정밀 치환, null-byte 0 확인). Pages 라이브 확인(raw.githubusercontent.com 200 → CDN 전파 대기 후 jwj-nick.github.io도 200).
  - ⚠️ 배포 repo에 이번 세션과 무관한 `high1/`(다른 프로젝트) 로컬 변경분이 있었음 — `git add`를 파일 단위로 정확히 지정해 건드리지 않고 넘어감.
  - 참고: `mid1/math1/print/index.html`(단원별 프린트 브라우징 페이지)은 소스 저장소에 대응 파일이 없는 **배포 전용 파일**임을 확인(생성 스크립트도 없음) — 이번엔 배포본만 직접 수정. 다음에 프린트 재생성할 때도 이 파일은 배포 repo에서만 갱신하면 됨.
- **다음**: 단원8(작도와 합동)부터 순차 재설계 착수 — u/g/p/d 4페이지 + 프린트, 단원7과 동일 패턴.

## Round 3 — 단원8(작도와 합동) 완성 (2026-08-20)

- Nick "나머지 단원 모두 진행바람" 승인에 따라 단원8을 단원7과 동일 패턴(u/g/p/d + 프린트)으로 완성.
- **u8.html(개념 8섹션)**: 도구역할→길이옮기기→**수직이등분선 슬라이더**(반지름 조절, 두 호 교점 실시간 계산)→각의이등분선(정적 SSS 증명 다이어그램)→삼각형작도조건(SSS/SAS/ASA + AAA/SSA 함정 텍스트)→합동표기순서(정적 대응 다이어그램)→내말로→다음.
- **g8.html(놀이터 4위젯)**: 합동판정관(SSS/SAS/ASA/SSA/AAA 5라운드) / 끼인각찾기(`MJ1.makeGeo`로 삼각형 그려 두 변 강조, 3라운드) / 컴퍼스작도시뮬레이터(반지름 슬라이더로 두 호의 교점 개수 실시간, AB=7cm 절반=3.5cm 챌린지) / 오류탐정(`math1_u08.json` 6종 큐레이션).
- **p8.html(연습 7유형×3=21문항)**: 작성 중 **논리 오류 1건 자체 발견·수정** — 유형2 문항(2) "벌림을 바꾸면 바뀔까요?"인데 정답이 "안 바뀌어요"로 되어 있어 질문·답이 모순이었음(원래 의도는 "벌림을 유지하면 항상 같은가"였는데 문구가 반대로 나감) → 질문을 "벌림을 유지하면 항상 같을까요?"로 정정, 앱·프린트 모두 재검증.
- **d8.html(심화탐구)**: "각의 삼등분은 왜 안 될까" — 이등분 반복 슬라이더(90°÷2ⁿ), `constr.trisection` 오개념 정면 타격.
- **검증**: p8.html에서 **버그 1건 자체 발견·수정** — `pick('c1','p21a'.replace('2','1'),...)` 문자열 표현식 잔재(의도는 `'p12a'`)가 남아있어 클릭 핸들러가 엉뚱한 버튼(`p11a`, 이미 사용 중)에 붙는 실제 오류였음 → 리터럴로 교체. 4페이지 전부 구문/id/null-byte 통과, Chrome 확장 연결되어 콘솔에러 0건 실측.
- **프린트**: `extract_math2_p.py` 재사용(unmatched=0, choice2=12). verify_bank V5 FAIL 2건 — `constr.tool-role` 오개념 카드가 counter/ask에서 "컴퍼스"를 실명으로 여러 번 거론해서, 답이 "컴퍼스"인 문항 2개와 충돌 → 두 문항의 misconceptions 태그만 제거(모든 문항에 오개념이 꼭 붙어야 하는 건 아님)로 해결. 회차 3개(a:유형1-3, b:유형4-5, c:유형6-7) 생성, PDF 6개, 2장 육안 확인 정상.
- **배포**: 소스(`math-story-telling`, 앱 `89da4e3`+버그수정 `904ee17`, 프린트 `b39ce7e`) · 배포(`jwj-nick.github.io`, 앱 `e8de76f`+수정 `0bdb638`, 프린트 `80dac32`) 전부 push 완료. `high1/`(무관한 다른 프로젝트) 로컬 변경분은 파일 단위 staging으로 계속 건드리지 않음.

## Round 4 — 단원9(다각형) 완성 (2026-08-21)

- 단원7·8과 동일 패턴(u/g/p/d + 프린트)으로 단원9 완성.
- **u9.html(개념 8섹션)**: 다각형정의(곡선판별)→구성요소(변=꼭짓점수)→**직접움직여봐요**(n슬라이더 4-8, 전체 대각선 세트를 이중루프로 실시간 렌더 + n(n−3)÷2 카운트)→삼각형내각외각(외각의 정리)→n각형내각의합(180×(n−2))→외각의합=360(항상성 강조)→내말로→다음.
- **g9.html(놀이터 4위젯)**: 대각선사냥꾼(정오각형 고정, 4라운드 변/대각선 판별 + 최종 "몇 개?" 챌린지) / 내각의합실험실(n슬라이더 3-8, 부채꼴 삼각분할 시각화 + 실시간 공식) / 외각레이서(n슬라이더 3-10, 항상 360° 강조) / 오류탐정(`math1_u09.json` 6종 큐레이션).
- **d9.html(심화탐구)**: "왜 정삼각형·정사각형·정육각형만 바닥을 채울까" — n슬라이더(3-10)로 정n각형 내각을 한 점에 부채꼴로 모으는 시뮬레이션, 360÷내각이 정수인지 실시간 판정(n=3,4,6만 성립 확인).
- **검증**: 4페이지 전부 구문/id/null-byte 통과, Chrome 확장 콘솔에러 0건 실측. 이번 회차는 로직 버그 없이 1차 작성에서 통과(u8의 논리모순·문자열치환 잔재 같은 이슈 재발 없음).
- **엔진 연결**: `concept.js` `PLAY_READY`에 9 추가, 허브 `index.html` 단원9 카드 활성화·캐시버전 `20260820b`·푸터 "단원7~9 오픈"로 갱신.
- **프린트**: `extract_math2_p.py` 재사용(`unmatched=0, choice2=1`). `30_content/problem_bank/math1/u09/practice.json` 21문항 신규 저작(전부 L/M, 2지선다 1문항 단답 재작성 — "원은 다각형인가요?"→"아닌지 쓰시오"). 기존 `misconceptions/math1_u09.json`(15개, 2026-07-29 기저작) 그대로 재사용. **이번엔 저작 전에 V5 답 누출을 13개 태그 전부 사전 점검**(u07/u08에서 걸렸던 실수 재발 방지) — verify_bank 1회 실행에 오류 0 (경고 4: 예/아니오 문항 1개의 V3/V4 경고 — u08 t01-03과 동일 패턴, 미사용 오개념 2개 안내). 회차 3개(a:유형1-3, b:유형4-6, c:유형7) 생성, PDF 6개, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 정상 — 2단 레이아웃·풀이공간·오개념 카드 정답 비노출 전부 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계 PDF 18개(t1~t7 + mockA/B)를 소스·배포 양쪽에서 삭제, 새 3개 회차로 완전 교체.
- **배포**: 소스(`math-story-telling`, 앱 `cad51f0`, 프린트 `df6b80a`) · 배포(`jwj-nick.github.io`, 앱 `e40e7ae`, 프린트 `e72be2b`) 전부 push 완료. `high1/` 무관 변경분은 이번에도 건드리지 않음.
- **다음**: 단원10(원과 부채꼴)부터 순차 진행.

## Round 5 — 단원10(원과 부채꼴) 완성 (2026-08-21)

- 단원7~9와 동일 패턴(u/g/p/d + 프린트)으로 단원10 완성. 소스는 구 `10_원과부채꼴/`(concepts.html 2편·types.md 7유형 목록)를 압축 참고 — 7유형 이름·핵심 개념을 그대로 계승.
- **π 답 입력 규약 신규 도입**: `MJ1.check`가 `parseFloat(raw)`로 앞부분 숫자만 파싱하는 특성을 활용해, π가 든 답은 전부 **"계수만 입력"**(예: 4π→4)로 통일. 질문 문구에 매번 "(계수만)" 안내 병기. 종합유형(색칠한 부분)의 복합식 답(예: `100 − 25π`)은 계수만으로 표현 불가하므로 텍스트 정규식 검사(두 숫자 모두 포함 확인)로 별도 처리 — 이 설계는 이후 원·부채꼴류 단원(11·12)에도 재사용 가능.
- **u10.html(개념 8섹션)**: 이어서(원의 정의: 다각형↔원 대비)→구성요소(호/현/할선/접선)→**직접움직여봐요**(r슬라이더 1-10, `G.el('circle',...)`로 원 렌더 — `makeGeo`엔 원 그리기 헬퍼가 없어 raw SVG circle 엘리먼트 직접 사용, 실시간 C=2πr·A=πr² 표시)→부채꼴·활꼴 구분→호의길이·넓이공식(비례 사고)→호현중심각관계(★함정)→내말로→다음.
- **g10.html(놀이터 4위젯)**: 원둘레넓이계산기(r슬라이더) / 부채꼴각도스캐너(θ슬라이더 10-350, SVG path arc로 웨지 렌더) / 호vs현경주(중심각60°·120° 두 원 나란히 비교, 예측형 2단계 질문) / 오류탐정(`math1_u10.json` 6종 큐레이션).
- **p10.html(연습 7유형×3=21문항)**: 원의구성요소·둘레와넓이·부채꼴활꼴구분·호의길이·부채꼴넓이·호현중심각관계(★참거짓 2문항+비례응용)·색칠한부분의넓이(정사각형−원/도넛/반원둘레).
- **d10.html(심화탐구)**: "π는 어떻게 처음 계산되었을까" — 아르키메데스 96각형 내접 슬라이더(n=3~60, 둘레÷지름을 실시간 계산해 π로 수렴하는 모습 확인).
- **검증**: 4페이지 전부 구문/id/null-byte 통과. **로컬 `python -m http.server` 검증 중 환경 이슈 발견·해결**: 과거 세션들이 남긴 유령 python 프로세스 10개가 여러 포트에서 계속 응답 중이었고, 그중 하나가 새 서버와 같은 포트에 얹혀 **math2의 옛 u10(피타고라스 정리) 페이지를 엉뚱하게 반환**(가짜 통과 위험) — 오늘 이전 python 프로세스 전부 kill 후 새 포트로 재검증, 4페이지 모두 올바른 제목·콘솔에러 0건 확인. 메모리 기록(`reference_stale_http_server_ports.md`) + `MEMORY.md`/project 메모리(`project_math1_app1_redesign.md`) 신규 작성.
- **엔진 연결**: `concept.js` `PLAY_READY`에 10 추가, 허브 `index.html` 단원10 카드 활성화·캐시버전 `20260821a`·푸터 "단원7~10 오픈"로 갱신.
- **프린트**: `extract_math2_p.py` 재사용(`unmatched=0, choice2=2`). 종합유형 2문항(정사각형−원, 반원둘레)은 앱이 이미 완성식 텍스트를 정답으로 채점하고 있어 추출된 atom을 그대로 answer_print로 사용(별도 재작성 불필요). `30_content/problem_bank/math1/u10/practice.json` 21문항 신규 저작, 기존 `misconceptions/math1_u10.json`(15개) 재사용, **저작 전 13개 태그 전부 사전 V5 누출 점검**(u07~09 교훈 적용) — verify_bank 1회 실행 오류 0 (경고 7: 예/아니오 문항 2개의 기존 패턴 + 미사용 오개념 4개 안내). 회차 3개(a:유형1-3, b:유형4-6, c:유형7) 생성, PDF 6개, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 정상 — 복합식 정답(100 − 25π, 5π+10) 렌더링·오개념 카드 정답 비노출 전부 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계 PDF 18개(t1~t7 + mockA/B)를 소스·배포 양쪽에서 삭제, 새 3개 회차로 완전 교체.
- **배포**: 소스(`math-story-telling`, 앱 `9553e91`, 프린트 `0542195`) · 배포(`jwj-nick.github.io`, 앱 `69f661e`, 프린트 `57505fb`) 전부 push 완료. `high1/` 무관 변경분은 이번에도 건드리지 않음.
- **다음**: 단원11(다면체와 회전체)부터 순차 진행.

## Round 6 — 단원11(다면체와 회전체) 완성 (2026-08-21)

- 단원7~10과 동일 패턴(u/g/p/d + 프린트)으로 단원11 완성. 소스는 구 `11_다면체와회전체/`(concepts.html 2편·types.md 7유형 목록, 케플러 역사 메모 포함)를 압축 참고.
- **u11.html(개념 8섹션)**: 이어서(다면체↔회전체 대비: 곧은 면 vs 곡면)→구성요소(V/E/F·오일러공식)→**직접움직여봐요**(n슬라이더 3-10, 3D 렌더 대신 각기둥/각뿔의 V,E,F 공식을 실시간 텍스트로 계산 — makeGeo에 입체 렌더 헬퍼가 없어 채택한 실용적 대안)→정다면체5종(★, 두 조건+왜 5종뿐인지)→회전체식별(직사각형/직각삼각형/사다리꼴/반원→각각의 회전체)→회전체의단면(★함정: 수직=원, 축단면=좌우대칭)→내말로→다음. 전개도(유형6)·종합(유형7)은 u페이지에서 생략(u10의 "마지막 유형만 생략" 관행보다 조금 더 압축 — 단원11이 다면체+회전체 두 큰 주제를 한 단원에 압축한 만큼, p11 연습에서 충분히 커버).
- **g11.html(놀이터 4위젯)**: 다면체탐지기(6개 입체 이름 보고 다면체/회전체 예측, 2단계 지연 렌더) / 정다면체각도실험실(정삼각형 개수 슬라이더 2-7, 부채꼴 웨지로 "한 꼭짓점 각의 합<360°" 시각화 — d9의 테셀레이션 패턴 재사용 + 정오각형 3개 사례 예측 질문) / 회전체재구성기(4개 평면도형 버튼 → 각 회전체의 2D 측면 실루엣을 `G.el('circle'/'ellipse'/'poly')`로 렌더) / 오류탐정(`math1_u11.json` 6종 큐레이션).
  - **자체 발견·수정 버그 2건**: (1) 정다면체각도실험실 위젯에서 "정오각형 3개→정다면체?" 질문의 오답 버튼(pl-no) 핸들러가 "맞았어요!"로 시작하며 스스로 모순되는 메시지를 출력하던 문제 → 정답 검증 로직 자체를 재점검해 오답 메시지를 정정, `bump()` 호출도 정답 버튼(pl-yes)에만 걸리도록 수정(원래는 슬라이더 이동만으로 매번 점수가 올라가던 버그). (2) 회전체재구성기의 직각삼각형 렌더 코드에 `rx:0,ry:0`인 죽은 `<ellipse>` 호출이 남아있어 제거.
- **p11.html(연습 7유형×3=21문항)**: 다면체분류·구성요소 / 각기둥각뿔각뿔대 V,E,F / 정다면체5종(★) / 회전체식별 / 회전체단면(★) / 회전체전개도(π 계수만 입력 규약 재사용) / 종합(오일러공식·결합입체).
- **d11.html(심화탐구)**: "왜 정다면체는 5종뿐인가 — 케플러의 우주 모형" — 정다각형(4종)×개수(3-6) 슬라이더 조합 실험실 + 1596년 케플러 「우주의 신비」 5정다면체 행성궤도 가설(틀렸지만 케플러 3법칙으로 이어진 역사) 서술.
- **검증**: 4페이지 전부 구문/id/null-byte 통과. **환경 정리**: 유닛10에서 발견한 유령 python 프로세스 이슈 재발 방지를 위해 검증 전 매번 `Get-Process python | Stop-Process` 선제 실행 + 새 포트 사용을 표준 절차로 굳힘. Chrome 콘솔 에러 4페이지 모두 0건 확인(제목 대조로 올바른 파일 서빙 재확인).
- **엔진 연결**: `concept.js` `PLAY_READY`에 11 추가, 허브 `index.html` 단원11 카드 활성화·캐시버전 `20260821b`·푸터 "단원7~11 오픈"로 갱신.
- **프린트**: `extract_math2_p.py` 재사용(`unmatched=0, choice2=2`). `30_content/problem_bank/math1/u11/practice.json` 21문항 신규 저작, 기존 `misconceptions/math1_u11.json`(15개) 재사용, 저작 전 14개 태그 전부 사전 V5 누출 점검(모두 통과, 재작업 없음 — u07~10 교훈이 이제 안정적으로 정착). verify_bank 1회 실행 오류 0 (경고 4: 예/아니오 문항 2개 기존 패턴 + 미사용 오개념 1개 안내). 회차 3개(a:유형1-3, b:유형4-6, c:유형7) 생성, PDF 6개, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계 PDF 18개(t1~t7 + mockA/B — 이번 단원은 mockA/B 이름이 "1차모의기본"/"2차모의보통"으로 다른 단원과 달랐음, 파일명에 특수문자 있어 `find -delete`로 정확히 처리)를 소스·배포 양쪽에서 삭제, 새 3개 회차로 완전 교체.
- **배포**: 소스(`math-story-telling`, 앱 `8ada27d`, 프린트 `e4ea3d4`) · 배포(`jwj-nick.github.io`, 앱 `d914387`, 프린트 `1317d07`) 전부 push 완료. `high1/` 무관 변경분은 이번에도 건드리지 않음.
- **다음**: 단원12(입체도형의 겉넓이와 부피)부터 순차 진행.

## Round 7 — 단원12(입체도형의 겉넓이와 부피) 완성 (2026-08-21)

- 단원7~11과 동일 패턴(u/g/p/d + 프린트)으로 단원12 완성. 소스는 구 `12_입체도형의겉넓이와부피/`(concepts.html 4편: 각기둥·각뿔·원기둥원뿔·구, types.md 7유형·아르키메데스 묘비 역사 메모)를 압축 참고.
- **u12.html(개념 8섹션)**: 이어서(겉넓이=포장지 vs 부피=담는양)→각기둥·각뿔(옆넓이=둘레×높이, 뿔은 밑면1개)→**직접움직여봐요**(밑넓이·높이 듀얼슬라이더, 각기둥부피 vs 각뿔부피(1/3) 실시간 비교)→원기둥·원뿔(★부피엔h, 겉넓이엔l)→구의공식(4πr²/⁴⁄₃πr³+반구)→회전체부피(단원11연계)→내말로→다음. 전개도류 유형은 없고(이 단원은 겉넓이·부피 자체가 7유형), 유형7(복합입체)만 u페이지에서 생략(기존 관행 유지).
- **g12.html(놀이터 4위젯)**: 1/3채우기실험실(각기둥vs각뿔 정적 실루엣 비교) / 원뿔hvsl구분기(r·h 듀얼슬라이더, 모선 l=√(r²+h²) 실시간 계산 + 빗변 시각화) / 1:2:3비율실험실(r슬라이더, 원뿔:구:원기둥 부피 실시간 비율 — 항상 1:2:3 확인) / 오류탐정(`math1_u12.json` 6종 큐레이션).
- **p12.html(연습 7유형×3=21문항)**: 각기둥·각뿔·원기둥·원뿔·구·회전체·복합입체(★). π 계수만 입력 규약(단원10 도입) 그대로 적용 — 이 단원이 가장 π 밀도가 높아 규약의 진가가 드러남.
- **d12.html(심화탐구)**: "아르키메데스가 묘비에 새겨 달라던 도형" — r슬라이더로 원뿔:구:원기둥 부피를 막대그래프로 시각화(1:2:3 고정), 원기둥=구+원뿔×2라는 아르키메데스 자신이 가장 자랑스러워한 발견 + 묘비 일화(키케로가 잡초에 덮인 묘비를 찾아냈다는 기록) 서술. d10(아르키메데스의 π)과 인물로 이어지는 서사 연결.
- **검증**: 4페이지 전부 구문/id/null-byte 통과, Chrome 콘솔 에러 0건(단원11에서 굳힌 "매 검증 전 python 프로세스 kill+새 포트" 절차 재적용, 이번엔 문제 없이 한 번에 통과).
- **엔진 연결**: `concept.js` `PLAY_READY`에 12 추가, 허브 `index.html` 단원12 카드 활성화·캐시버전 `20260821c`·푸터 "단원7~12 오픈"로 갱신.
- **프린트**: `extract_math2_p.py` 재사용(`unmatched=0, choice2=0` — 이번 단원은 2지선다 재작성이 전혀 필요 없었음). `30_content/problem_bank/math1/u12/practice.json` 21문항 신규 저작, 기존 `misconceptions/math1_u12.json`(15개) 재사용, 저작 전 사전 V5 누출 점검(특히 카드 자체의 check 예시와 숫자가 겹치지 않도록 문항 수치를 의도적으로 다르게 선택) — verify_bank 1회 실행 오류 0 (경고 5: 전부 미사용 오개념 안내, 10/15 사용). 회차 3개(a:유형1-3, b:유형4-6, c:유형7) 생성, PDF 6개, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 정상 — 1:2:3 비율·복합식 답 렌더링 전부 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계 PDF 18개(t1~t7 + mockA/B)를 소스·배포 양쪽에서 삭제, 새 3개 회차로 완전 교체.
- **배포**: 소스(`math-story-telling`, 앱 `b903027`, 프린트 `4af4515`) · 배포(`jwj-nick.github.io`, 앱 `5ec1055`, 프린트 `cec0070`) 전부 push 완료. `high1/` 무관 변경분은 이번에도 건드리지 않음.
- **다음**: 단원13(자료의 정리와 해석)부터 진행 — 2학기(07~13) 마지막 단원.

## Round 8 — 단원13(자료의 정리와 해석) 완성 · 2학기 전 단원 완주 (2026-08-21)

- 단원7~12와 동일 패턴(u/g/p/d + 프린트)으로 단원13 완성. 소스는 구 `13_자료의정리와해석/`(concepts.html 4편: 줄기와잎·도수분포표·히스토그램·상대도수, types.md 7유형)을 압축 참고. **인물**: 30_content/units/13/meta.json에 이미 배정된 플로렌스 나이팅게일(로즈 다이어그램, 크림전쟁 1858) — d13에서 활용.
- 이 단원은 기하가 아닌 **통계 단원**이라 π 대신 **소수(상대도수)**가 핵심 답 형식 — u/g/p 전체에서 `MJ1.check`(parseFloat)로 소수 비교, `MJ1.checkNum`(parseInt)은 정수 전용이라 쓰면 안 된다는 점을 u13.html 작성 중 자체 발견·수정(c5 상대도수 체크포인트에서 처음엔 `checkNum`을 썼다가 0.2가 parseInt로 0이 되는 버그를 잡아 `check`로 교체).
- **u13.html(개념 7섹션)**: 이어서(줄기와잎그림)→도수분포표(계급·계급값·이상/미만)→**직접움직여봐요**(계급크기·도수합 듀얼슬라이더, 히스토그램 막대 실시간 렌더+넓이=크기×도수합)→도수분포다각형→상대도수(★두집단비교함정)→내말로→다음. (통계 단원 특성상 도형류보다 섹션 1개 적은 7섹션으로 자연스럽게 축약, 유형6·7은 u페이지에서 생략)
- **g13.html(놀이터 4위젯)**: 줄기잎탐정(고정 데이터셋 3문제 순차 풀이) / 도수분포표빈칸채우기실험실(슬라이더로 빈칸 도수 맞춰 합계 30 만들기) / 상대도수비교저울(가·나 모둠 전체·도수 4중 슬라이더로 상대도수 실시간 비교) / 오류탐정(`math1_u13.json` 6종 큐레이션).
- **p13.html(연습 7유형×3=21문항)**: 줄기와잎·도수분포표기본·도수분포표만들기·히스토그램·도수분포다각형·상대도수·종합해석(★).
- **d13.html(심화탐구)**: "통계 그림 한 장이 병원을 바꿨어요" — 나이팅게일의 로즈 다이어그램(coxcomb chart) 구조를 12개 부채꼴로 단순화 재현(실제 역사적 수치 대신 "예시 그림"으로 명시해 정확성 리스크 회피) + 크림전쟁 위생개혁 서사. d10(아르키메데스)·d12(아르키메데스)에 이어 세 번째 실제 역사 인물 딥다이브.
- **검증**: 4페이지 전부 구문/id/null-byte 통과, Chrome 콘솔 에러 0건(python 프로세스 kill+새 포트 절차로 재검증 없이 한 번에 통과).
- **엔진 연결**: `concept.js` `PLAY_READY`에 13 추가, 허브 `index.html` 단원13 카드 활성화·캐시버전 `20260821d`. **2학기 전체 완주**이므로 허브 문구를 "지금 만드는 중"→"전 단원 완성!"으로, 푸터를 "2학기(7~13) 전 단원 오픈 · 1학기 재설계 중"으로 갱신 — 단원7~12 때와 달리 이번엔 섹션 상태 문구 자체를 변경.
- **프린트 — 이번 단원의 핵심 사건**: `extract_math2_p.py` 재사용(`unmatched=0, choice2=2`) 후 `practice.json` 초안 저작 중 **V5 사전 점검에서 4건의 실질적 충돌 발견** — 통계 단원 오개념 카드들이 "20이상30미만→계급값25", "50이상60미만에 60이 들어가나요→안들어가요", "0.15/0.25/0.35 상대도수" 같은 구체적 예시를 counter/ask에 이미 쓰고 있어서, 처음 설계한 문항이 그 예시와 **문항 전체가 통째로 겹치는 사례**가 3건, 정답 문자열이 다른 예시 안에 숫자 부분열로 우연히 섞여 드는 사례가 1건(상대도수 0.3의 정규화값 "03"이 카드의 "0.15,0.25,0.35" 나열 안에 부분열로 존재) 나왔음. **이번엔 verify_bank 실행 전에 손으로 다 짚어서 잡음** — u07~09 시기엔 verify_bank가 잡아준 뒤 사후 수정했던 것과 달리, 이번엔 사전 점검만으로 1회 실행에 오류 0을 달성. 해결을 위해 **p13.html 자체를 4곳 수정**(계급 구간 번호·상대도수 예시 수치 변경) → 재검증(구문/id/null-byte/Chrome 콘솔) → 재배포 → 재추출 순으로 진행, 소스·배포 양쪽에 fix 커밋 별도 기록. 최종 `30_content/problem_bank/math1/u13/practice.json` 21문항 확정, 기존 `misconceptions/math1_u13.json`(15개, 13/15 사용) 재사용, verify_bank 오류 0(경고 4: 예/아니오 문항 2개 기존 패턴 + 미사용 오개념 2개 안내). 회차 3개(a:유형1-3, b:유형4-6, c:유형7) 생성, PDF 6개, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계 PDF 18개(t1~t7 + mockA/B)를 소스·배포 양쪽에서 삭제, 새 3개 회차로 완전 교체. 배포 `print/index.html`의 u13 블록은 파일의 **마지막 단원**이라 다음 `<div class="unit">` 마커가 없어 종료 태그(`</div></body></html>`)까지 통째로 잡히는 특이 케이스 — 첫 줄만 골라 치환하는 방식으로 안전하게 처리.
- **배포**: 소스(`math-story-telling`, 앱 `b1fe174`+수정 `5a88632`, 프린트 `c370871`) · 배포(`jwj-nick.github.io`, 앱 `38bf87a`+수정 `5a69cc2`, 프린트 `76b247d`) 전부 push 완료. `high1/` 무관 변경분은 이번에도 건드리지 않음.
- **🎉 2학기(단원7~13) 전체 완료**: 앱 4종(u/g/p/d) × 7단원 = 28개 페이지, 프린트 유형세트 3개 × 7단원 = 21개 PDF 회차(문제지+해답묶음 42개 파일). 전부 새 `app1/` 엔진(math2 app2 수준 압축) 기반으로 재설계·검증·배포 완료.
- **다음**: Nick 확인 대기 후 1학기(단원1~6) 착수 여부 결정 — "나머지 단원 모두 진행바람" 승인이 2학기 한정이었는지, 1학기까지 포함하는지 재확인 필요. 계획 문서(`00_project_hub/20_plan/0818_math1_upgrade_plan.md`)의 P3(1학기) 단계 착수 전 Nick 리뷰 권장.

## Round 9 — Nick 승인(1학기 계속) + 단원1(소인수분해) 완성 (2026-08-21)

- **Nick 승인**: AskUserQuestion으로 확인 → "네, 계속 진행 (Recommended)" — 1학기(1~6)도 2학기와 동일 방식(u/g/p/d 앱 + 프린트)으로 매 단원 확인 없이 자율 진행. 계획 doc P3 착수 확정.
- 단원7~13과 동일 패턴으로 단원1(소인수분해) 완성 — **1학기 재설계 착수**. 소스는 구 `01_소인수분해/`(concepts.html 5편: 소수와합성수·거듭제곱·소인수분해·최대공약수·최소공배수, types.md 7유형)를 압축 참고. **인물**: CLAUDE.md 단원별 인물 배정표에 이미 배정된 에라토스테네스(체, BC 276~194) — d1에서 활용.
- **u1.html(개념 7섹션)**: 이어서(소수·합성수 정의, 2학기 끝→1학기 시작 브릿지)→거듭제곱(밑·지수)→**직접움직여봐요**(n슬라이더 2-100, 약수 목록+개수 실시간 나열→소수/합성수 자동 판정)→소인수분해(나눗셈반복법)→최대공약수·최소공배수(작은지수/큰지수)→관계식(A×B=gcd×lcm)→내말로→다음. (통계 단원처럼 자연수론도 도형보다 섹션 1개 적은 7섹션, 유형4·7은 u페이지에서 생략)
- **g1.html(놀이터 4위젯)**: 소수사냥꾼(6문제 순차 판별) / 소인수분해나무(슬라이더+실시간 소인수분해, JS로 직접 소인수분해 알고리즘 구현) / GCD·LCM저울(두 수 슬라이더로 gcd·lcm 실시간 계산+서로소 판정) / 오류탐정(`math1_u01.json` 6종 큐레이션).
- **p1.html(연습 7유형×3=21문항)**: 소수합성수판별·거듭제곱·소인수분해실행·약수의개수·최대공약수·최소공배수·GCD LCM관계식.
- **d1.html(심화탐구)**: "2200년 전 소수를 골라내는 체" — 에라토스테네스의 체를 2~100 그리드로 인터랙티브 구현(버튼 클릭마다 다음 소수의 배수를 지워나가며 진행, √100=10까지만 확인하면 충분한 이유 설명) + 알렉산드리아 도서관 사서 일화.
- **검증**: 4페이지 전부 구문/id/null-byte 통과, Chrome 콘솔 에러 0건. d1.html의 체 알고리즘(표준 최적화 버전, p²부터 마킹)을 손으로 직접 트레이스해 25개 소수가 정확히 남는지 사전 검증.
- **엔진 연결**: `concept.js` `PLAY_READY`에 1 추가, 허브 `index.html` — **1학기 섹션을 "재설계 중이에요"→"지금 만드는 중"으로 전환**, 단원1 카드 활성화, 캐시버전 `20260821e`, 푸터 "2학기 전 단원 오픈 · 1학기 단원1 오픈"로 갱신.
- **프린트 — 이번 단원의 핵심 사건**: `extract_math2_p.py` 재사용(`unmatched=0, choice2=2`). 저작 전 V5 사전 점검에서 **구조적 함정 발견** — 답이 "소수"인 문항을 관련 오개념 카드에 태그하면 거의 항상 FAIL: 소수/합성수를 다루는 카드들의 counter·ask 문구 자체가 설명 중에 "소수"라는 낱말을 필연적으로 포함하기 때문(예: "소수의 조건은…", "소수를 죽 늘어놓으면…"). 이번엔 verify_bank 실행 전에 이 패턴을 미리 알아채 **답이 "소수"인 문항은 무태그 처리**, "서로소" 답은 `answer_print`를 "맞아요"로 두고 "서로소예요"류 표현은 `answer_accept`로 분리하는 우회 기법으로 사전 회피 — 1회 실행에 오류 0 달성. `30_content/problem_bank/math1/u01/practice.json` 21문항 확정, 기존 `misconceptions/math1_u01.json`(15개, 12/15 사용) 재사용. 회차 3개(a:유형1-3, b:유형4-6, c:유형7) 생성, PDF 6개, 2장(a회차 문제지, c회차 해답묶음) 육안 확인 정상 — 지수 위첨자(²³⁴⁵) 렌더링 전부 정상.
- **구버전 정리**: 예전 9문항/유형(L3M3H3) 체계 PDF 18개(t1~t7 + mockA/B)를 소스·배포 양쪽에서 삭제, 새 3개 회차로 완전 교체.
- **배포**: 소스(`math-story-telling`, 앱 `a42d85b`, 프린트 `27213ad`) · 배포(`jwj-nick.github.io`, 앱 `ed9aa88`, 프린트 `433db88`) 전부 push 완료. `high1/` 무관 변경분은 이번에도 건드리지 않음.
- **다음**: 단원2(정수와 유리수)부터 1학기 순차 진행.
- **다음**: 단원9(다각형)부터 이어서 진행.
